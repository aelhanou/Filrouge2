const { userModel } = require("./user")
const { categoryModel } = require("./categorie")
const { bookModel } = require("./book")
const { channelModel } = require("./channel")
const { courseModel } = require("./course")
const { episodeModel } = require("./episode")
const { techModel } = require("./tech")

module.exports = {
    userModel,
    categoryModel,
    bookModel,
    channelModel,
    courseModel,
    episodeModel,
    techModel
}